#ifndef BANNER_H
#define BANNER_H

#include <stdio.h>

/*
 * Prints the application banner and a short usage guide.
 * Output is written to the given stream (typically stdout).
 */
void chat_banner_print(FILE *output_stream);

#endif
